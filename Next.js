### To set up the project:

1. Create a new Next.js project:
```bash
npx create-next-app@latest admybrand-insights --typescript --tailwind --eslint
```

2. Add required dependencies:
```bash
npm install @tanstack/react-table chart.js csv-writer jspdf react-chartjs-2 date-fns
npm install -D @types/react-table @types/chart.js
```

3. Set up shadcn/ui (optional but recommended):
```bash
npx shadcn-ui@latest init
```

4. Copy all the provided files into your project structure
